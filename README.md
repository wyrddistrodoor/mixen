# mixen
Wyrt in the dirt. 
What does your Monstera desire?

## GitHub Pages
This repository deploys to GitHub Pages with the workflow in `.github/workflows/pages.yml` on pushes to `main` and manual runs.
In repository **Settings → Pages**, set **Source** to **GitHub Actions**. After the first successful run, the site URL appears in the `github-pages` deployment environment.
